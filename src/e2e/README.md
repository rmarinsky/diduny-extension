# Extension E2E (Future)

This folder is intentionally pre-created.

Planned later:

- Playwright config (`playwright.config.ts`) and helper fixtures
- Opt-in test scripts for Chrome extension flows
- Real browser scenarios for dictation/translation

For now, keep all extension tests out of default CI and run only when extension MVP flows are ready.
